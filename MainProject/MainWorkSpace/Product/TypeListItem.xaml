<UserControl x:Class="MainProject.MainWorkSpace.Product.TypeListItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:MainProject.MainWorkSpace.Product"
             mc:Ignorable="d" 
             Height="60" Width="350">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <materialDesign:PackIcon Kind="Ballot"  Width="30" Height="30" Foreground="{StaticResource MainColor}" VerticalAlignment="Center" Margin="10,0"/>
        <TextBlock Text="{Binding Type, FallbackValue='Tất cả'}"
                       Grid.Column="1"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Center"
                       Margin="10 0"
                       Foreground="#BD8722"
                       FontSize="20"
                       FontWeight="Bold" />
    </Grid>
</UserControl>
